[project]
name = "sql-alembic-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Demonster-01", email = "kac2035@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.20.0",
    "psycopg2-binary>=2.9.13",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
sql-alembic-learning = "sql_alembic_learning:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
